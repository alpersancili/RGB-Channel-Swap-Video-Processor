# RGB-Channel-Swap-Video-Processor
This project is a simple video processing application built with OpenCV and Gradio. It allows users to upload a video and automatically converts each frame from BGR to RGB color channels, then exports the processed video. The app has also been deployed on Hugging Face Spaces for easy online usage.
